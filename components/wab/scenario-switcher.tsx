"use client";

// Prototype-only scenario switcher is disabled now that the app
// uses a real backend. Keep a no-op stub to avoid layout changes.

export function ScenarioSwitcher() {
  return null;
}
